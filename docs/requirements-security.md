
# Вимоги до безпеки (Фаза аналізу)

## 1. Область застосунку
Flask-вебдодаток з аутентифікацією, пошуком і демонстраційною БД (SQLite).

## 2. Функціональні вимоги безпеки
- **AUTH-1**: Авторизація користувачів для доступу до /profile.
- **AUTH-2**: Блокування після N невдалих логінів (TODO).
- **STOR-1**: Паролі зберігати із сіллю та стійким хешем (PBKDF2/Argon2) (наразі: *порушено для тренування*).
- **LOG-1**: Аудитна подія логіну/невдалих спроб.
- **CONF-1**: Секретні ключі лише в секретах CI/CD/ENV.

## 3. Нефункціональні вимоги
- Відповідність: OWASP ASVS L1-L2 (базові пункти).
- Продуктивність: контроль витрат ресурсу під час DAST.
- Експлуатаційність: наявність процедур тріажу і виправлення.

## 4. Загрози (огляд)
- SQLi в ендпоінтах пошуку.
- XSS на відображенні неперевірених параметрів.
- Weak auth (словникові атаки), некоректне зберігання паролів.
- Витік секретів через логи/репозиторій.

## 5. Цілі безпеки
- Конфіденційність (C): дані акаунтів і сесії.
- Цілісність (I): коректність результатів пошуку й профілів.
- Доступність (A): сервіс має бути доступним під час тестування.

## 6. Початкові ризики
- R1: SQLi → високий.
- R2: XSS → середній.
- R3: Weak auth → високий.
- R4: Dependency vulns → середній.

## 7. Висновок етапу аналізу
Проєкт базується на моделі DevSecOps. Задокументовані вище вимоги та список ризиків із файлу risk-register.csv приймаються як базовий стан (Baseline) для подальшої інтеграції SAST та DAST.


